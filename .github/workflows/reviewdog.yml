name: reviewdog
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  reviewdog:
    name: Code Review
    runs-on: ubuntu-latest
    steps:
    - uses: reviewdog/action-setup@v1
      with:
        reviewdog_version: latest