name: reviewdog
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  reviewdog:
    name: Code Review
    steps:
    - uses: reviewdog/action-setup@v1
      with:
        reviewdog_version: latest